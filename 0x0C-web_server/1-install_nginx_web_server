#!/usr/bin/env bash
# installs nginx and configure it to listen on port 80
# print "Hello World"

apt-get update -y
apt-get install nginx -y
echo 'Hello World!' | tee -i /var/www/html/index.nginx-debian.html
